require 'spec_helper'

describe PayDesign do
  it 'has a version number' do
    expect(PayDesign::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(false)
  end
end
